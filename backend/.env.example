# PostgreSQL (optional - falls back to file storage if not set)
# Start with: docker compose up -d
DATABASE_URL=postgresql://ppc_user:ppc_password@localhost:5432/ppc_dashboard

# Server
PORT=8787
HOST=127.0.0.1

# CORS
CORS_ORIGIN=http://localhost:5173